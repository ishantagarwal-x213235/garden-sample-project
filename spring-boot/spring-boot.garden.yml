kind: Module
type: templated
template: spring-service-module-template
name: spring-boot
#disabled: ${environment.name != 'dev'} #Uncomment it will not work and report the error (missing environment key)
inputs:
  jdkVersion: "8" #by defualt available in schema.json but ModuleTemplate failed to find it.